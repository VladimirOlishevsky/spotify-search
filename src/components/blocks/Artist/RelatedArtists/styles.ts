
import { makeStyles } from '@mui/styles';

export const getStyles = makeStyles({
    root: {
        display: 'flex',
        flexDirection: 'column',
        gap: 16
    },
    relatedArtists: {
        display: 'flex',
        gap: 16,
        flexWrap: 'wrap'
    },
})

