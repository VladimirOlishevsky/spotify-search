
import { makeStyles } from '@mui/styles';

export const getStyles = makeStyles({
    root: {
        display: 'flex',
        flexDirection: 'column',
        gap: 16
    },
    songsWrapper: {
        display: 'flex',
        flexDirection: 'column',
        gap: 8
    },
})

